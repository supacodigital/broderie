# Pas de VITE_API_URL — Axios utilise /api/v1 relatif, proxié par Vite en dev
VITE_STRIPE_PUBLIC_KEY=pk_test_...
VITE_GOOGLE_CLIENT_ID=change_me.apps.googleusercontent.com

# Optionnelles (fallbacks dans le code) :
# VITE_SITE_URL — URL canonique pour les balises SEO (hreflang/canonical). Défaut : https://broderie.ch
# VITE_APP_VERSION — version affichée dans le log console au démarrage. Défaut : « (non définie) »
